# Stage 0: packages required by the currently implemented code.
# Versions match the working Python 3.11.9 environment used to train/serve this project.
# CLIP is loaded via Hugging Face Transformers (CLIPModel / CLIPProcessor), not a separate clip package.

# Core ML
torch==2.13.0
transformers==5.14.1
datasets==5.0.0
accelerate==1.14.0
scikit-learn==1.8.0
joblib==1.5.3
xgboost==3.2.0
numpy==2.4.2
pandas==2.3.3

# Images / HTTP (CLIP embeddings, review-image download)
Pillow==12.1.1
requests==2.34.2
tqdm==4.67.3

# API (existing FastAPI service; uvicorn is the documented process runner)
fastapi==0.141.1
pydantic==2.13.4
uvicorn==0.52.3

# Experiment tracking and data versioning (existing MLflow scripts + DVC GDrive remote)
mlflow==3.15.1
dvc[gdrive]==3.67.1

# Testing
pytest==9.1.1
httpx==0.28.1
